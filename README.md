Project uses CRA(Create React App) and acts as a wrapper over vis.js to create an interactive timeline which can work for single/multiple groups with any number of events organized by event start time.
